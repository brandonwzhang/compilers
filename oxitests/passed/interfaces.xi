use oxi_test2

class DatBoi {
	oshit: int[]

	init(): DatBoi {
		oshit = "waddup"
		return this
	}
}

createDatBoi(): DatBoi {
	return new DatBoi.init()
}

main(args: int[][]) {
	println("nice")
	println(unparseInt(420))
	if (jihun()) {
		frog: DatBoi = hereCome()
		println("nice2")
	}
    // DOES NOT RUN BY ITSELF
}

